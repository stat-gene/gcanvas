% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca-ancestry.R
\name{pca.ancestry}
\alias{pca.ancestry}
\title{Estimate ancestry membership from a reference-aligned PCA}
\usage{
pca.ancestry(
  pc,
  eval = NULL,
  pc.use = 1:10,
  id.col = "IID",
  label.data = NULL,
  label.col = NULL,
  subgroup.data = NULL,
  subgroup.col = NULL,
  label.ref = NULL,
  target.id = NULL,
  target.only = FALSE,
  k = 11L,
  pve.cutoff = 0.95,
  pp.cutoff = 0.5,
  prior = "empirical",
  prior.user = NULL,
  outlier.k = 11L,
  outlier.score = 0.95
)
}
\arguments{
\item{pc}{A PCA result containing both reference and target samples
(typically the output of \code{\link[=pca.projection]{pca.projection()}}).}

\item{eval}{Optional eigenvalue vector (defaults to \code{pc$eigenval}).}

\item{pc.use}{Integer vector of PCs to use (default \code{1:10}).}

\item{id.col}{Column name carrying the sample id.}

\item{label.data}{A \code{data.frame} of \verb{(id, ancestry)} for reference samples.}

\item{label.col}{Column name carrying the ancestry label in \code{label.data}.}

\item{subgroup.data, subgroup.col}{Optional finer-grained subgroup labels.}

\item{label.ref}{Optional fallback label for unmatched reference rows.}

\item{target.id}{Optional explicit target-sample ids (otherwise inferred
from missing labels).}

\item{target.only}{Logical. Return only target-sample rows in the result.}

\item{k}{Neighborhood size used for posterior estimation.}

\item{pve.cutoff, pp.cutoff}{Posterior thresholds for assignment / inclusion.}

\item{prior}{Prior mode (\code{"empirical"} or \code{"uniform"}).}

\item{prior.user}{Optional named prior overrides.}

\item{outlier.k, outlier.score}{Outlier-detection neighborhood size and score.}
}
\value{
A list with per-sample ancestry assignments / probabilities and
any QC diagnostics.
}
\description{
Assigns each target sample to (or scores its membership across) the
reference ancestry labels by comparing its position in PC space to the
QC'd reference populations.
}
