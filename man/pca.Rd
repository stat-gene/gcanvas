% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{pca}
\alias{pca}
\title{Principal component analysis of PLINK genotype data}
\usage{
pca(
  bfile = NULL,
  pfile = NULL,
  plink = "plink2",
  plink.out = FALSE,
  show.plinklog = TRUE,
  threads = 4L,
  chrom = 1:22,
  keep = NULL,
  maf = 0.05,
  geno = 0.02,
  indep.window = 50L,
  indep.step = 5L,
  indep.r2 = 0.1,
  pca.mode = "approx",
  pca.count = 20L,
  plink.version = "auto",
  silent = FALSE
)
}
\arguments{
\item{bfile, pfile}{PLINK1 bfile prefix or PLINK2 pfile prefix.}

\item{plink}{Path to the \code{plink2} binary (default \code{"plink2"}).}

\item{plink.out}{Logical or path. Persist the PLINK log/outputs.}

\item{show.plinklog}{Logical. Stream PLINK's log to the R console.}

\item{threads}{Integer thread count passed to PLINK.}

\item{chrom}{Chromosomes to include (default autosomes \code{1:22}).}

\item{keep}{Optional sample-id file for \code{--keep}.}

\item{maf, geno}{Variant QC thresholds.}

\item{indep.window, indep.step, indep.r2}{LD-pruning controls
(\code{--indep-pairwise}).}

\item{pca.mode}{One of \code{"approx"} (default) or \code{"exact"}.}

\item{pca.count}{Number of PCs to extract.}

\item{plink.version}{PLINK version selector (\code{"auto"}, \code{"plink"}, \code{"plink2"}).}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A list with \code{pc} (sample scores), \code{eval} (eigenvalues), \code{meta}
(run metadata), and other auxiliary slots.
}
\description{
Runs PCA via PLINK2 (\code{--pca}) on a bfile or pfile, returning a list with
the PC scores, eigenvalues, and the resolved PLINK invocation. Provides
threading, output-directory, and QC-pass-through controls.
}
