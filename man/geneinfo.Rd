% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtf.R
\name{geneinfo}
\alias{geneinfo}
\title{Look up gene metadata by symbol or Ensembl id}
\usage{
geneinfo(
  gene,
  build = 38L,
  gtf = NULL,
  type = c("auto", "symbol", "ensg"),
  all.possible = TRUE,
  info.update = NULL,
  replace = FALSE
)
}
\arguments{
\item{gene}{Character vector of gene symbols or Ensembl gene ids, or a
\code{data.frame}/\code{data.table} containing one such column.}

\item{build}{Genome build (\code{37} or \code{38}).}

\item{gtf}{Optional path to a \code{gtf2rds()}-produced cache to use instead of
the package-bundled tracks.}

\item{type}{Input type detector: \code{"auto"}, \code{"symbol"}, or \code{"ensg"}.}

\item{all.possible}{Logical. Return every matching annotation row instead
of one row per query.}

\item{info.update}{Optional named list / table of overrides to merge in.}

\item{replace}{Logical. If \code{TRUE}, overrides replace existing annotation
values; otherwise they only fill missing ones.}
}
\value{
A \code{data.table} with one row per resolved annotation.
}
\description{
Returns gene-level annotation (chromosome, start/end, strand, biotype)
for the requested gene(s) by symbol or Ensembl gene id, using the bundled
or user-supplied GTF cache.
}
