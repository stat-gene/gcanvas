% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regional-tracks.R
\name{regional.track}
\alias{regional.track}
\title{Build gene/exon tracks for a regional plot}
\usage{
regional.track(
  gtf_bgz = NULL,
  chrom,
  pos.range,
  y.max,
  lead_pos,
  tracks = NULL,
  keep_biotype = NULL,
  gene_max_levels = 6L,
  gene_max_n = 50L,
  gene_force = character(),
  gene_force_label = NULL,
  gene_add = NULL,
  gene_force_ignore_max_levels = TRUE,
  biotype_priority = c("protein_coding", "lncRNA", "miRNA"),
  lncrna_symbol_only = FALSE,
  include_other_biotypes = FALSE,
  plot.width = NULL,
  units = "cm",
  force_edge_genes = TRUE
)
}
\arguments{
\item{gtf_bgz}{Path to the bgzipped GTF (the \code{.rds} cache is located
alongside it). Optional when \code{tracks} is supplied.}

\item{chrom}{Chromosome of the region.}

\item{pos.range}{Numeric length-2 vector \verb{(start, end)} in bp.}

\item{y.max}{Y-axis maximum from the association panel (for proportioning).}

\item{lead_pos}{Position of the lead variant (used for label spacing).}

\item{tracks}{Optional in-memory annotation object (the same list shape as
produced by \code{\link[=gtf2rds]{gtf2rds()}}, e.g. the bundled \link{tracks.b37} / \link{tracks.b38}).
When supplied, gene/exon records are taken from it directly and \code{gtf_bgz}
is ignored (no tabix query is performed).}

\item{keep_biotype}{Character vector of biotypes to keep (default
\code{c("protein_coding", "lncRNA")} unless \code{include_other_biotypes} is set).}

\item{gene_max_levels}{Maximum number of vertical levels for gene layout.}

\item{gene_max_n}{Maximum number of genes drawn.}

\item{gene_force}{Symbols always drawn, even past \code{gene_max_n}/levels.}

\item{gene_force_label}{Optional label override for \code{gene_force}.}

\item{gene_add}{Additional gene metadata to overlay.}

\item{gene_force_ignore_max_levels}{Logical. Allow forced genes past the
level cap.}

\item{biotype_priority}{Sort order applied when picking which genes win.}

\item{lncrna_symbol_only}{Logical. Restrict lncRNAs to symbol-named entries.}

\item{include_other_biotypes}{Logical. Allow non-default biotypes.}

\item{plot.width, units}{Plot-size hints used for label placement.}

\item{force_edge_genes}{Logical. Keep genes whose edges touch the region.}
}
\value{
A list of layout tables (gene, exon, label) consumed by the
regional plotter.
}
\description{
Queries a \code{gtf2rds()} annotation cache for the requested region, applies
biotype filters and gene-force / gene-add rules, lays out genes across
available levels, and returns the layout tables that \code{\link[=regional]{regional()}} (or a
bespoke regional figure) can render below the association track.
}
