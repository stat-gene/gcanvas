% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pilot.R
\name{get.pilot}
\alias{get.pilot}
\title{Downsample summary statistics for a pilot plot}
\usage{
get.pilot(
  data,
  snp.col = "SNP",
  chrom.col = "CHR",
  pos.col = "POS",
  p.col = "P",
  y.col = NULL,
  lead = NULL,
  lead.flank = 5e+05,
  threshold = 5e-08,
  threshold.flank = 5e+05,
  n.variant = 30000L,
  per.chrom.min = 50L,
  method = c("stratified", "uniform"),
  seed = 23L,
  keep.boundary = TRUE,
  keep.chr.minp = TRUE,
  return.type = c("auto", "data.table", "data.frame"),
  silent = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} of summary statistics.}

\item{snp.col, chrom.col, pos.col, p.col}{Column names in \code{data}.}

\item{y.col}{Optional alternative y-column.}

\item{lead}{Optional table of pre-identified lead variants.}

\item{lead.flank}{Window kept around each lead variant (bp).}

\item{threshold}{Significance threshold for automatic peak retention.}

\item{threshold.flank}{Window around each above-threshold variant.}

\item{n.variant}{Approximate total number of variants in the output.}

\item{per.chrom.min}{Minimum variants kept per chromosome.}

\item{method}{One of \code{"stratified"} (default) or \code{"uniform"}.}

\item{seed}{Integer seed for reproducibility.}

\item{keep.boundary}{Logical. Always include the chromosome-boundary rows.}

\item{keep.chr.minp}{Logical. Always include the per-chromosome minimum-p row.}

\item{return.type}{Output container: \code{"auto"}, \code{"data.table"}, or \code{"data.frame"}.}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A \code{data.table} of the sampled rows.
}
\description{
Returns a stratified subset of \code{data} that preserves all variants near
lead signals and significance peaks while thinning the rest, suitable for
fast iteration on plot styling before running the full dataset.
}
