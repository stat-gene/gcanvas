% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{pca.projection}
\alias{pca.projection}
\title{Project new samples onto a reference PCA}
\usage{
pca.projection(
  ref,
  target,
  plink = "plink2",
  plink.format = "pfile",
  plink.out = FALSE,
  id.aligned = FALSE,
  force = FALSE,
  show.plinklog = TRUE,
  threads = 4L,
  chrom = 1:22,
  maf = 0.05,
  geno = 0.02,
  indep.window = 50L,
  indep.step = 5L,
  indep.r2 = 0.1,
  pca.mode = "approx",
  pca.count = 20L,
  plink.version = "auto",
  silent = FALSE
)
}
\arguments{
\item{ref}{A reference PCA result (output of \code{\link[=pca]{pca()}}).}

\item{target}{A PLINK bfile/pfile prefix or precomputed target genotype matrix.}

\item{plink}{Path to the \code{plink2} binary.}

\item{plink.format}{\code{"pfile"} or \code{"bfile"}.}

\item{plink.out}{Logical or path. Persist PLINK outputs.}

\item{id.aligned}{Logical. Assume target sample ids already align with \code{ref}.}

\item{force}{Logical. Force re-projection even when cached intermediates exist.}

\item{show.plinklog}{Logical. Stream PLINK's log to the R console.}

\item{threads}{Integer thread count.}

\item{chrom}{Chromosomes to include (default autosomes).}

\item{maf, geno}{Variant QC thresholds.}

\item{indep.window, indep.step, indep.r2}{LD-pruning controls.}

\item{pca.mode}{One of \code{"approx"} (default) or \code{"exact"}.}

\item{pca.count}{Number of PCs to project.}

\item{plink.version}{PLINK version selector.}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A list mirroring the \code{\link[=pca]{pca()}} output with \code{pc} containing the
projected scores for \code{target}.
}
\description{
Uses the variant loadings from a reference \code{\link[=pca]{pca()}} run to compute PC
scores for samples in a separate genotype file (target), enabling apples-
to-apples comparison to the reference population's PC space.
}
