% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toy-data.R
\name{toy.gwas}
\alias{toy.gwas}
\title{Simulate a toy GWAS summary-statistics table}
\usage{
toy.gwas(
  n.sample = 10000L,
  n.ancestry = 1L,
  n.snp = 10000L,
  n.causal = 3L,
  iid.prefix = NULL,
  build = 38L,
  chr = 1:22,
  var.list = NULL,
  causal.window = 200000L,
  causal.nsnp = 120L,
  causal.min.sig = 8L,
  p.threshold = 5e-08,
  p.floor = 1e-50,
  eaf.range = c(0.01, 0.5),
  h2 = 0.2,
  causal.mix = c(0.55, 0.3, 0.15),
  ld.decay.bp = 30000,
  bfile = TRUE,
  overwrite = TRUE,
  show.progress = TRUE,
  seed = NULL
)
}
\arguments{
\item{n.sample}{Sample size used to scale standard errors.}

\item{n.ancestry}{Number of ancestries to simulate. \verb{>1} returns a list.}

\item{n.snp}{Approximate number of variants.}

\item{n.causal}{Number of causal loci.}

\item{iid.prefix}{Prefix for synthetic sample IDs.}

\item{build}{Genome build (\code{37} or \code{38}).}

\item{chr}{Chromosomes to populate.}

\item{var.list}{Optional vector of SNP IDs to use as the variant scaffold.}

\item{causal.window, causal.nsnp, causal.min.sig}{Causal-locus geometry.}

\item{p.threshold, p.floor}{P-value threshold for "significant" tagging and
the floor used when truncating extreme p-values.}

\item{eaf.range}{Effect-allele frequency draw range.}

\item{h2}{Per-locus heritability.}

\item{causal.mix}{Mixing weights for effect-direction patterns at causal loci.}

\item{ld.decay.bp}{Approximate LD decay distance in bp.}

\item{bfile}{Logical or path prefix. \code{TRUE} (default) writes a default
PLINK1 bfile next to the working directory; a character path writes to
that prefix; \code{FALSE} skips bfile generation.}

\item{overwrite}{Logical. Overwrite existing bfile / output.}

\item{show.progress}{Logical. Show a progress bar.}

\item{seed}{Integer seed for reproducibility.}
}
\value{
A \code{data.table} of summary stats (single ancestry) or a named
list of such tables (multi-ancestry). Carries \code{attr(., "gcanvas_meta")}
with run parameters.
}
\description{
Generates a synthetic GWAS for quick examples and tests. Produces a
\code{data.table} of summary stats (\code{SNP}, \code{CHR}, \code{POS}, \code{A1}, \code{A2}, \code{EAF},
\code{BETA}, \code{SE}, \code{Z}, \code{P}, \code{N}, \code{is_lead}, ...) and -- when \code{bfile} is set --
a matching PLINK1 bfile suitable for downstream LD / PCA workflows.
}
\details{
Supports single-ancestry (default, returns a \code{data.table}) and multi-
ancestry mode (\code{n.ancestry > 1}, returns a named list of \code{data.table}s
plus a merged PLINK fileset).
}
\seealso{
\code{\link[=toy.eqtl]{toy.eqtl()}} for matching eQTL-style data,
\code{\link[=get.lead]{get.lead()}} to extract lead variants from the result.
}
