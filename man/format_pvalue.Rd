% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stats.R
\name{format_pvalue}
\alias{format_pvalue}
\title{Format p-values for \code{ggtext::geom_richtext()} / Markdown labels}
\usage{
format_pvalue(
  x,
  log10p = FALSE,
  digits = 2L,
  plain.cutoff = NULL,
  html = TRUE,
  times = "&times;"
)
}
\arguments{
\item{x}{P-values (numeric or character) or, when \code{log10p = TRUE},
base-10-log p-values (i.e. \code{log10(p)}, \strong{not} natural log).}

\item{log10p}{Logical. If \code{TRUE}, \code{x} is treated as \code{log10(p)}. Default \code{FALSE}.}

\item{digits}{Mantissa digits to keep (default \code{2}).}

\item{plain.cutoff}{Numeric \verb{(0, 1]} or \code{NULL}. P-values at or above this
value are rendered as plain decimals (e.g. \code{"0.05"}) instead of
scientific notation. \code{NULL} (default) means "always use scientific".}

\item{html}{Logical. If \code{TRUE} (default), emit HTML-rich output using
\verb{<sup>} and the \code{times} glyph -- ready for
\code{\link[ggtext:geom_richtext]{ggtext::geom_richtext()}}. If \code{FALSE}, emit a plain ASCII form,
\code{"<mantissa> x 10^<exp>"} (e.g. \code{"3.20 x 10^-8"}).}

\item{times}{Character. The multiplication glyph in HTML mode. Defaults
to the HTML entity \code{"&times;"}. Ignored when \code{html = FALSE}.}
}
\value{
Character vector of the same length as \code{x}.
}
\description{
Converts numeric, character, or \code{log10(p)} inputs into a typeset
scientific-notation string like \code{"3.2 &times; 10<sup>-8</sup>"} (HTML,
default — suitable for \code{\link[ggtext:geom_richtext]{ggtext::geom_richtext()}} /
\code{\link[ggtext:element_markdown]{ggtext::element_markdown()}}) or \code{"3.2 x 10^-8"} (plain text).
}
\details{
Tiny p-values that would underflow under \code{as.numeric()} are routed through
\code{\link[=log10c]{log10c()}} when the input is character, or supplied directly via
\code{log10p = TRUE}. Either way precision is preserved.
}
\examples{
\dontrun{
  library(ggplot2); library(ggtext)
  ggplot(df, aes(x, y, label = format_pvalue(P))) +
    geom_richtext()
  # log10(p) input (e.g. from -negLog10P in a Manhattan plot)
  format_pvalue(-7.3, log10p = TRUE)
  # plain-text fallback (e.g. for base graphics or copy-paste)
  format_pvalue(5e-8, html = FALSE)
}
}
