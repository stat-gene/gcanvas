% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca-ancestry.R
\name{pca.refqc}
\alias{pca.refqc}
\title{Reference-PCA QC for ancestry estimation}
\usage{
pca.refqc(
  pc,
  eval = NULL,
  pc.use = 1:10,
  id.col = "IID",
  label.data = NULL,
  label.col = NULL,
  k.grid = seq(11, 61, by = 2),
  pve.cutoff = 0.95,
  pp.cutoff = 0.5,
  prior = "empirical",
  prior.user = NULL,
  loop = TRUE,
  outlier.k = 11L,
  outlier.score = 0.95
)
}
\arguments{
\item{pc}{A reference PCA result (output of \code{\link[=pca]{pca()}}).}

\item{eval}{Optional eigenvalue vector (defaults to \code{pc$eigenval}).}

\item{pc.use}{Integer vector of PCs to use (default \code{1:10}).}

\item{id.col}{Column name carrying the sample id.}

\item{label.data}{Optional \code{data.frame} of labels (one row per sample).}

\item{label.col}{Column name carrying the ancestry/population label.}

\item{k.grid}{Integer vector of \code{k} values to scan adaptively.}

\item{pve.cutoff, pp.cutoff}{Posterior thresholds for assignment / inclusion.}

\item{prior}{Prior mode (\code{"empirical"} or \code{"uniform"}).}

\item{prior.user}{Optional named prior overrides.}

\item{loop}{Logical. Iterate QC until convergence.}

\item{outlier.k, outlier.score}{Outlier-detection neighborhood size and score.}
}
\value{
A list with the QC-passed PC table, the dropped IDs, and the
selected \code{param.best} value.
}
\description{
Removes within-population outliers from a reference PCA result so that
downstream \code{\link[=pca.ancestry]{pca.ancestry()}} estimates are not biased by mislabeled or
admixed samples in the reference. Selects the QC depth \code{k} adaptively.
}
