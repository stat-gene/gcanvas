% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{calcld}
\alias{calcld}
\title{Compute LD (r and r-squared) between variants}
\usage{
calcld(
  snp = NULL,
  chrom = NULL,
  pos = NULL,
  data = NULL,
  a1 = NULL,
  snp.col = "SNP",
  chrom.col = "CHR",
  pos.col = "POS",
  a1.col = "A1",
  flank = "250kb",
  r2 = TRUE,
  matrix = FALSE,
  pairwise = c("auto", TRUE, FALSE),
  mode = c("auto", "flank", "set"),
  ld.bfile = NULL,
  ld.rds = NULL,
  ld.cache.dir = NULL,
  threads = 4L,
  silent = FALSE
)
}
\arguments{
\item{snp}{Character vector of SNP ids (or a table).}

\item{chrom, pos}{Coordinate-based input alternative to \code{snp}.}

\item{data}{Optional \code{data.frame} providing both lookups and metadata.}

\item{a1}{Optional A1 mapping for sign-aware alignment.}

\item{snp.col, chrom.col, pos.col, a1.col}{Column names in \code{data}.}

\item{flank}{Window size (numeric bp or e.g. \code{"250kb"}) for flank mode.}

\item{r2}{Logical. Return r-squared instead of signed r.}

\item{matrix}{Logical. Return a square matrix in addition to / instead of
the pair table.}

\item{pairwise}{One of \code{"auto"}, \code{TRUE}, \code{FALSE} — pairwise vs. lead-vs-set.}

\item{mode}{One of \code{"auto"}, \code{"flank"}, \code{"set"} — region resolution.}

\item{ld.bfile, ld.rds, ld.cache.dir}{Reference panel pointers.}

\item{threads}{Integer thread count.}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A list containing \code{data} (pair table) and/or \code{R} (matrix), the
variant \code{a1} mapping, and a \code{meta} slot of run metadata.
}
\description{
Flexible LD calculator covering single-variant lookup, pairwise mode, set
mode (full square matrix), and lead-vs-region mode. Resolves variants via
SNP id or coordinates, attaches the configured bigsnpr LD reference, and
optionally writes the long-form pair table to disk.
}
