% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get-lead.R
\name{get.lead}
\alias{get.lead}
\title{Identify lead variants in summary statistics}
\usage{
get.lead(
  data,
  snp.col = "SNP",
  chrom.col = "CHR",
  pos.col = "POS",
  p.col = "P",
  z.col = "Z",
  beta.col = "BETA",
  se.col = "SE",
  method = "p",
  flank = 5e+05,
  flank.locus = flank,
  p.threshold = 5e-08,
  silent = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} of summary statistics.}

\item{snp.col, chrom.col, pos.col}{Column names for SNP id, chromosome,
and position.}

\item{p.col, z.col, beta.col, se.col}{Optional statistic columns used by the
ranking method.}

\item{method}{One of \code{"p"}, \code{"z"}, \code{"beta_se"} — what to rank by.}

\item{flank}{Suppression window around each lead (bp or e.g. \code{"500kb"}).}

\item{flank.locus}{Optional locus-level suppression window (defaults to \code{flank}).}

\item{p.threshold}{Maximum p-value for a row to be eligible as a lead.}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A \code{data.table} of lead variants and the rows they suppress.
}
\description{
Greedy lead-variant selection: scans rows by increasing p-value (or
decreasing |Z|), accepts the most significant variant as a lead, then
suppresses everything within \code{window} bp on the same chromosome, and repeats.
}
