% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pca.R
\name{pca.plot}
\alias{pca.plot}
\title{Plot PCA results}
\usage{
pca.plot(
  pc,
  eval = NULL,
  pc.use = "all",
  id.col = "IID",
  group.data = NULL,
  group.col = NULL,
  group.title = NULL,
  group.color = NULL,
  group.shape = NULL,
  group.size = NULL,
  group.stroke = NULL,
  point.color = "grey20",
  point.size = 1.5,
  point.stroke = 1,
  point.shape = 16,
  alpha = 0.65,
  outlier = NULL,
  ambiguous = NULL,
  fade = NULL,
  line.sd = FALSE,
  line.sd.color = NULL,
  line.sd.type = "dashed",
  line.sd.linewidth = 0.3,
  legend.title = NULL,
  legend.position = c("bottom", "top", "left", "right"),
  equal.lim = c("panel", "global", "none"),
  projection.helper = FALSE,
  projection.group = NULL
)
}
\arguments{
\item{pc}{A PCA result list (output of \code{\link[=pca]{pca()}} or \code{\link[=pca.projection]{pca.projection()}}).}

\item{eval}{Optional eigenvalue vector for variance-explained axis labels.}

\item{pc.use}{Which PCs to plot (\code{"all"} or integer vector).}

\item{id.col}{Sample-id column name in \code{pc}.}

\item{group.data, group.col, group.title, group.color, group.shape, group.size, group.stroke}{Category / grouping controls.}

\item{point.color, point.size, point.stroke, point.shape, alpha}{Base point styling.}

\item{outlier, ambiguous, fade}{Optional outlier / ambiguous-sample annotations.}

\item{line.sd, line.sd.color, line.sd.type, line.sd.linewidth}{Standard-deviation
ellipse / line controls.}

\item{legend.title, legend.position}{Legend controls.}

\item{equal.lim}{One of \code{"panel"}, \code{"global"}, \code{"none"} — axis-limit policy.}

\item{projection.helper, projection.group}{Helpers for plotting projected
target samples on top of a reference PCA.}
}
\value{
A \code{ggplot} object (or list of them for multi-panel modes).
}
\description{
Renders 2D / 3D / facet-grid PC scatter plots with optional category
coloring, density overlays, projection comparison, and reference shading.
}
