% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/toy-data.R
\name{toy.eqtl}
\alias{toy.eqtl}
\title{Simulate a toy eQTL summary-statistics table}
\usage{
toy.eqtl(
  base = NULL,
  n.sample = 500L,
  n.snp = 10000L,
  n.causal = 3L,
  iid.prefix = NULL,
  build = 38L,
  chr = 1:22,
  causal.window = 200000L,
  causal.nsnp = 120L,
  causal.min.sig = 12L,
  p.threshold = 5e-08,
  p.floor = 1e-50,
  eaf.range = c(0.01, 0.5),
  h2 = 0.35,
  causal.mix = c(0.55, 0.3, 0.15),
  ld.decay.bp = 30000,
  gene.per.locus = 3L,
  gene.prefix = "ENSGTOY",
  gtf = NULL,
  gtf.rds = NULL,
  cis.window = 1000000L,
  cis.decay.bp = 50000L,
  focal.cis.scale = c(0.35, 0.65),
  nonfocal.cis.scale = c(0.08, 0.28),
  focal.causal.boost = c(2, 4),
  nonfocal.causal.boost = c(0.3, 1.1),
  trans.noise.ratio = 0.15,
  bfile = FALSE,
  overwrite = TRUE,
  show.progress = TRUE,
  seed = NULL,
  silent = FALSE
)
}
\arguments{
\item{base}{Optional output of \code{\link[=toy.gwas]{toy.gwas()}} to reuse the variant scaffold.}

\item{n.sample}{Sample size used to scale standard errors.}

\item{n.snp, n.causal, causal.window, causal.nsnp, causal.min.sig}{Causal-
locus geometry.}

\item{iid.prefix, build, chr, p.threshold, p.floor, eaf.range, h2, causal.mix, ld.decay.bp}{Same meaning as in \code{\link[=toy.gwas]{toy.gwas()}}.}

\item{gene.per.locus}{Number of genes drawn per causal locus.}

\item{gene.prefix}{Prefix used to synthesize Ensembl-style gene ids.}

\item{gtf}{Optional path to a bgzipped + tabix-indexed GTF -- if supplied,
gene names / TSS are pulled from real annotation instead of being made up.}

\item{gtf.rds}{Optional \code{gtf2rds()} cache (alternative to \code{gtf}).}

\item{cis.window, cis.decay.bp, focal.cis.scale, nonfocal.cis.scale, focal.causal.boost, nonfocal.causal.boost, trans.noise.ratio}{Cis-window geometry and effect-size mixing for focal vs. non-focal genes.}

\item{bfile}{Logical / path. See \code{\link[=toy.gwas]{toy.gwas()}} for behavior.}

\item{overwrite, show.progress, seed}{Same meaning as in \code{\link[=toy.gwas]{toy.gwas()}}.}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A \code{data.table} of eQTL summary statistics with one row per
(gene, variant) pair. Carries \code{attr(., "gcanvas_meta")} with run params.
}
\description{
Generates a synthetic eQTL dataset compatible with the \code{\link[=regional]{regional()}} /
\code{\link[=manhattan]{manhattan()}} / \code{\link[=calcld]{calcld()}} surface. Adds gene-level columns
(\code{gene_id}, \code{gene_name}, \code{gene_tss}, \code{gene_locus_id}, \code{gene_is_focal},
\code{is_lead_gene}, ...) on top of the standard GWAS layout.
}
\details{
Can either build a new variant scaffold from scratch or piggyback on an
existing \code{\link[=toy.gwas]{toy.gwas()}} table via \verb{base = <toy.gwas result>} so the eQTL
and GWAS share variants and bfile.
}
\seealso{
\code{\link[=toy.gwas]{toy.gwas()}} for the matching GWAS-style generator.
}
