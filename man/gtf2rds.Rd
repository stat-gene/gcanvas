% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtf.R
\name{gtf2rds}
\alias{gtf2rds}
\title{Convert a bgzipped + tabix-indexed GTF into an \code{.rds} annotation cache}
\usage{
gtf2rds(
  gtf.bgz,
  build = 38L,
  out.rds = NULL,
  overwrite = FALSE,
  compress = "xz"
)
}
\arguments{
\item{gtf.bgz}{Path to a bgzipped GTF with a \code{.tbi} alongside.}

\item{build}{Genome build (\code{37} or \code{38}).}

\item{out.rds}{Output \code{.rds} path. If \code{NULL}, derived from \code{gtf.bgz}.}

\item{overwrite}{Logical. Overwrite an existing output file.}

\item{compress}{Compression used by \code{\link[=saveRDS]{saveRDS()}}.}
}
\value{
Invisibly returns the path to the written \code{.rds}.
}
\description{
Reads gene and exon records from \code{gtf.bgz} for each contig listed in the
tabix index, normalizes chromosome names, and writes a single \code{.rds} file
containing a list with \code{genes} and \code{exons} data.tables. The resulting cache
is what \code{\link[=regional.track]{regional.track()}} and \code{\link[=geneinfo]{geneinfo()}} consume.
}
