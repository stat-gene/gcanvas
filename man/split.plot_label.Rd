% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-utils.R
\name{split.plot_label}
\alias{split.plot_label}
\title{Split a plot into point-only and label-only layers}
\usage{
\method{split}{plot_label}(x, label.point = c("legend.only", "thin", "keep"), keep.n = 96L)
}
\arguments{
\item{x}{A \code{ggplot} object, for example the return value of \code{\link[=manhattan]{manhattan()}}.}

\item{label.point}{How much of the point-layer data to keep in the label
plot: \code{"legend.only"} (default) retains only a minimal representative subset
needed to reproduce the legend and hides the points themselves; \code{"thin"}
retains up to \code{keep.n} representative points; \code{"keep"} retains all point
data.}

\item{keep.n}{Maximum number of points to retain when \code{label.point = "thin"}.}
}
\value{
A list of two \code{ggplot} objects: \code{point} (point layers only, with
non-point decorations drawn transparent) and \code{label} (all layers, with the
point-layer data reduced according to \code{label.point}, and panel/plot/legend
backgrounds made transparent so it can be overlaid on the rasterised
\code{point} plot). Save \code{label} with a transparent device background
(e.g. \code{ggsave(..., bg = "transparent")}) when compositing.
}
\description{
Splits a \code{ggplot} into two plots that share an identical layout: one holding
only the point layers (the heavy scatter data, e.g. a full genome-wide cloud)
and one holding the labels, axes, reference lines, and legend as lightweight
vector graphics. Render the point plot as a raster and overlay the label plot
as vector to keep text crisp while keeping the file small. Non-point
decorations on the point plot are made transparent (not removed) so the two
plots align exactly when overlaid at the same device size.
}
\details{
\code{gcanvas} performs no rasterisation itself; this function only separates the
layers so you can rasterise the point plot with the tool of your choice (for
example \code{ggrastr::rasterise()}, or by saving it to PNG) and then combine it
with the vector label plot.

This is a plain function (the dot in its name does not denote an S3 method
for \code{\link[base:split]{base::split()}}).

For memory efficiency the returned plots hold independent \emph{data} but delegate
each layer's \emph{structure} (geom/stat/mapping) to \code{x} by reference (via
\code{\link[ggplot2:ggproto]{ggplot2::ggproto()}}), avoiding a full serialize of the environment-heavy
layer objects. They render identically to a full deep copy (verified
pixel-for-pixel). The only consequence is that saving a returned plot with
\code{\link[=saveRDS]{saveRDS()}} pulls in \code{x}'s layer structure and is therefore large; save the
rendered image (PNG/PDF) instead, which is unaffected.
}
