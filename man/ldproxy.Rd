% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{ldproxy}
\alias{ldproxy}
\title{LD proxy variants for a query SNP}
\usage{
ldproxy(
  x,
  rsq = 0.8,
  window = "500kb",
  ld.rds = NULL,
  ld.bfile = NULL,
  snp.col = "SNP",
  chrom.col = "CHR",
  pos.col = "POS",
  ld.cache.dir = NULL,
  threads = 4L
)
}
\arguments{
\item{x}{Query SNP id (character of length 1) or a table of queries.}

\item{rsq}{Minimum r-squared threshold (default 0.8).}

\item{window}{LD search window. Numeric (bp) or a string like \code{"500kb"}.}

\item{ld.rds, ld.bfile}{Reference panel pointers (RDS or PLINK bfile).}

\item{snp.col, chrom.col, pos.col}{Column names in the query table.}

\item{ld.cache.dir}{Optional directory for the LD reference cache.}

\item{threads}{Integer thread count.}
}
\value{
A \code{data.table} of proxies with id, chromosome, position, alleles,
r/r-squared, and distance.
}
\description{
Returns variants in linkage disequilibrium with the query SNP above the
requested r-squared threshold within \code{window} bp, using the configured
bigsnpr LD reference (or a precomputed RDS / matrix).
}
