% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/align.R
\name{ld.align}
\alias{ld.align}
\title{Align LD r values to a reference allele}
\usage{
ld.align(x, reference, snp.col = "SNP", a1.col = "A1", a1.x = NULL)
}
\arguments{
\item{x}{A pair \code{data.frame}/\code{data.table} with \code{SNP}, \code{SNP2}, \code{r} columns,
a list with a \code{data} or \code{R} slot from \code{\link[=calcld]{calcld()}}, or a numeric matrix
with \code{dimnames}.}

\item{reference}{A \code{data.frame}/\code{data.table} with \code{SNP} and \code{A1} columns
describing the desired reference allele per variant.}

\item{snp.col}{Name of the SNP id column in \code{reference} / \code{x}.}

\item{a1.col}{Name of the A1 column in \code{reference} / \code{x}.}

\item{a1.x}{Optional A1 mapping for \code{x} (named character vector, list, or
table). Required when \code{x} is a matrix or a bare pair table.}
}
\value{
An object matching the input shape with r values aligned, plus
bookkeeping about which variants were retained.
}
\description{
Sign-flips entries of a long-form LD r pair table or a square r matrix so
that values are consistent with a reference effect allele. Expects signed
r (not r-squared).
}
