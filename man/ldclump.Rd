% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld.R
\name{ldclump}
\alias{ldclump}
\title{LD-based clumping of association summary statistics}
\usage{
ldclump(
  data,
  rsq = 0.2,
  p.threshold = 5e-08,
  window = "500kb",
  ld.rds = NULL,
  ld.bfile = NULL,
  snp.col = "SNP",
  chrom.col = "CHR",
  pos.col = "POS",
  p.col = NULL,
  z.col = NULL,
  prune.window = 200L,
  prune.step = 50L,
  mode = c("auto", "clump", "prune"),
  ld.cache.dir = NULL,
  threads = 4L,
  silent = FALSE
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} of summary statistics with SNP,
chromosome, position, and p-value columns.}

\item{rsq}{Minimum r-squared for a variant to be clumped into an index.}

\item{p.threshold}{Maximum p-value for an index variant.}

\item{window}{Search window. Numeric (bp) or a string like \code{"500kb"}.}

\item{ld.rds, ld.bfile}{Reference panel pointers (RDS or PLINK bfile).}

\item{snp.col, chrom.col, pos.col, p.col, z.col}{Column names in \code{data}.}

\item{prune.window, prune.step}{Window/step for variant pruning mode (bp/SNP).}

\item{mode}{One of \code{"auto"}, \code{"clump"}, \code{"prune"} — algorithm selector.}

\item{ld.cache.dir}{Optional directory for the LD reference cache.}

\item{threads}{Integer thread count.}

\item{silent}{Logical. Suppress progress notes.}
}
\value{
A \code{data.table} of clumps with the index variant and a list-column
of clumped variants.
}
\description{
Performs the standard PLINK-style clumping algorithm: while there are
variants below \code{p.threshold}, pick the smallest-p one as an index variant
and absorb everything within \code{window} bp at r-squared >= \code{rsq} into its clump.
}
