% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-regional.R
\name{regional}
\alias{regional}
\title{Regional association plot}
\usage{
regional(
  data,
  snp = NULL,
  snp2 = NULL,
  chrom = NULL,
  pos = NULL,
  snp.col = "SNP",
  chrom.col = "CHR",
  pos.col = "POS",
  p.col = "P",
  flank = 5e+05,
  pos.range = NULL,
  pos.unit = c("mb", "kb", "base"),
  alpha = 1,
  y.col = NULL,
  x.title = NULL,
  y.title = NULL,
  title = NULL,
  ld.col = "ld",
  ld2.col = "ld2",
  beta.col = NULL,
  a1.col = NULL,
  a2.col = NULL,
  direction = FALSE,
  direction.a1 = NULL,
  label.snp = NULL,
  label.top.n = 0L,
  label.col = "SNP",
  label.size = 3,
  label.seed = 23L,
  label.mode = c("smart", "top", "off"),
  y.max = NULL,
  y.breaks = NULL,
  y.rescale.at = NULL,
  y.rescale.ratio = 0.25,
  y.rescale.breaks = NULL,
  threshold = 5e-08,
  threshold.color = "grey20",
  threshold.type = NULL,
  threshold.linewidth = 0.7,
  plot.width = NULL,
  units = "cm",
  build = 38L,
  build.gwas = NULL,
  liftover = NULL,
  liftover.chain = NULL,
  ld.matrix = NULL,
  ld.rds = NULL,
  ld.bfile = NULL,
  ld.cache.dir = NULL,
  threads = 4L,
  ld.color = NULL,
  snp.color = "#CC2936",
  snp2.color = "#08415C",
  shared.mode = c("off", "split", "single"),
  shared.color = "#702B9E",
  shared.cutoff = 0.6,
  shared.alpha = 1,
  legend.position = c("bottom", "top", "left", "right", "left.outside", "right.outside"),
  gtf = NULL,
  hl.gene = NULL,
  hl.gene.color = NULL,
  hl.gene.size = 3,
  show.gene = TRUE,
  show.gene.label = TRUE,
  show.exon = TRUE,
  show.omit_label = TRUE,
  gene.max_row = 8L,
  gene.max_n = 50L,
  gene.add = NULL,
  gene.force = character(),
  gene.force.label = NULL,
  gene.force.ignore_max_row = TRUE,
  biotype.keep = c("protein_coding", "lncRNA"),
  biotype.priority = c("protein_coding", "lncRNA"),
  biotype.keep.all = FALSE,
  exon.size = 1,
  snp.color.missing = FALSE,
  snp2.color.missing = FALSE,
  biotype.keep.default = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame}/\code{data.table} of summary statistics.}

\item{snp, snp2}{Lead variant id(s) — \code{snp2} enables the second-track overlay.}

\item{chrom, pos}{Optional coordinates if \code{snp} is not provided.}

\item{snp.col, chrom.col, pos.col, p.col}{Column names in \code{data}.}

\item{flank}{Window size (bp) when only a lead variant is given.}

\item{pos.range}{Explicit \verb{(start, end)} window override.}

\item{pos.unit}{X-axis units: \code{"mb"}, \code{"kb"}, or \code{"base"}.}

\item{alpha}{Point alpha.}

\item{y.col, x.title, y.title, title}{Optional axis / title overrides.}

\item{ld.col, ld2.col}{LD columns to look up.}

\item{beta.col, a1.col, a2.col, direction, direction.a1}{Direction-of-effect
options.}

\item{label.snp, label.top.n, label.col, label.size, label.seed, label.mode}{Variant-labeling controls.}

\item{y.max, y.breaks, y.rescale.at, y.rescale.ratio, y.rescale.breaks}{Y-axis
range and break-and-rescale controls.}

\item{threshold, threshold.color, threshold.type, threshold.linewidth}{Significance-line controls.}

\item{plot.width, units}{Plot-size hints for label placement.}

\item{build, build.gwas}{Genome build of the GTF (and of \code{data} if different).}

\item{liftover, liftover.chain}{Liftover controls when builds differ:
\code{liftover} is the path to the \code{liftOver} executable (or \code{NULL} to use one
on \code{PATH}); chains are auto-resolved from the bundled files.}

\item{ld.matrix, ld.rds, ld.bfile, ld.cache.dir, threads}{LD reference inputs.}

\item{ld.color, snp.color, snp2.color}{Color scales for LD and points.}

\item{shared.mode, shared.color, shared.cutoff, shared.alpha}{Shared-signal
overlay controls (when both \code{snp} and \code{snp2} are given).}

\item{legend.position}{Where the legend is placed.}

\item{gtf}{Optional path to a GTF / \code{gtf2rds} cache.}

\item{hl.gene, hl.gene.color, hl.gene.size}{Gene-highlight controls.}

\item{show.gene, show.gene.label, show.exon, show.omit_label}{Visibility toggles
for the gene track.}

\item{gene.max_row, gene.max_n, gene.add, gene.force, gene.force.label, gene.force.ignore_max_row}{Gene-track layout controls (passed to \code{\link[=regional.track]{regional.track()}}).}

\item{biotype.keep, biotype.priority, biotype.keep.all}{Biotype filters.}

\item{exon.size}{Exon-thickness in the gene track.}

\item{snp.color.missing, snp2.color.missing}{Logical. Treat missing LD as
"color missing" instead of dropping the point.}

\item{biotype.keep.default}{Logical. Use the default biotype keep set.}
}
\value{
A \code{ggplot} object combining the association panel and gene tracks.
}
\description{
Draws a regional Manhattan-style plot for one locus: variants colored by
LD r-squared to the lead variant, optional second-track overlay (e.g. eQTL),
direction-of-effect glyphs, credible-set highlighting, and gene/exon tracks
supplied by \code{\link[=regional.track]{regional.track()}}.
}
\details{
The lead variant can be specified via \code{snp}, by \code{chrom} + \code{pos}, or
omitted entirely. When neither a SNP nor coordinates pin a lead variant
(or when the requested \code{snp} is not in \code{data}), the function falls back
to a no-lead mode: it draws the region defined by \code{pos.range} without a
highlighted lead point, lead-driven LD computation
(\code{ld.matrix} / \code{ld.rds} / \code{ld.bfile}) is skipped with a warning, and
point coloring uses precomputed \code{ld.col} / \code{ld2.col} columns if present.
}
