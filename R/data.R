#' Bundled gene / exon annotation tracks (GRCh37)
#'
#' Pre-rendered gene and exon layout tables for human genome build 37,
#' consumed by [regional()] and [regional.track()] when no custom GTF is
#' supplied. Built from a GENCODE GTF via [gtf2rds()].
#'
#' @format A list with `gene` and `exon` `data.table`s (and a `meta` list).
#' @keywords datasets
#' @source Derived from GENCODE GRCh37 annotation.
#' @name tracks.b37
"tracks.b37"

#' Bundled gene / exon annotation tracks (GRCh38)
#'
#' Pre-rendered gene and exon layout tables for human genome build 38,
#' consumed by [regional()] and [regional.track()] when no custom GTF is
#' supplied. Built from a GENCODE GTF via [gtf2rds()].
#'
#' @format A list with `gene` and `exon` `data.table`s (and a `meta` list).
#' @keywords datasets
#' @source Derived from GENCODE GRCh38 annotation.
#' @name tracks.b38
"tracks.b38"
